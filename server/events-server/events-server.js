
  
  const changeScreen = (socket, db, io) => {

  };  

  const randomKeys = (socket, db, io) => {
    
  };

  const verifyKeys = (socket, db, io) => {
    
  };

  module.exports = {
    changeScreen,
    randomKeys,
    verifyKeys
  };